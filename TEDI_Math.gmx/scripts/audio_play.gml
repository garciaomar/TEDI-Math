/// audio_play(soundid, prioriry, loops)

// Arguments
var soundid = argument0;
var priority = argument1;
var loops = argument2;

// Play the desired sound
audio_play_sound(soundid, priority, loops);
